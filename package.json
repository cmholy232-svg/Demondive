{
  "name": "demondive",
  "private": true,
  "version": "1.0.0-alpha.7.3c",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "verify": "npm run build && node scripts/verify-alpha73c-recovery.mjs"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "typescript": "^5.9.2",
    "vite": "^7.0.6"
  }
}
